# install Heroku for mac

1. install

    おもむろにhomebrewでインストール。

    ``` bash
    brew tap heroku/brew && brew install heroku
    ```

2. show version

    バージョンを確認する。

    ``` bash
    heroku --version
    ```
